#include "main.h"
#include <string.h>
/**
 * _strlen - return the length of a string
 * @s: pointer to the string
 * Return: length of string
 */
int _strlen(char *s)
{
int len = strlen(s);
return (len);
}

