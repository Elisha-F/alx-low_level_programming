#include "main.h"
#include <ctype.h>
/**
 * _isupper - checks uppercase character
 * @c: variable character c
 * Return: 1 if c is uppercase, and ) otherwise
 */

int _isupper(int c)
{

if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
