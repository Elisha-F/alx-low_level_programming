#include <stdio.h>
/**
 * main - Entry point for main
 *
 * Return: Always 0 (Succes)
 */
int main(void)
{
int number;
for (number == 0; number < 10; number++)
{
printf("%d", number);
}
printf("\n");
}
