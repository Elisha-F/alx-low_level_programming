#include "main.h"
#include <string.h>
/**
 * _strcpy - copies the string pointed by scr to dest
 * @src: Source array
 * @dest: destination array
 * Return: array dest
 */
char *_strcpy(char *dest, char *src)
{
strcpy(dest, src);
return (dest);
}
