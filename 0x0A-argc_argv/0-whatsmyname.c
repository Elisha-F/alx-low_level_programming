#include "main.h"

/**
 * main - prints its name followed by a new line
 * @argc: int variable
 * @argv: array of char type
 * Return: 0 Always
 */

int main(int  argc, char *argv[])
{
argc--;
printf("%s\n", argv[0]);
return (0);
}
