#include "main.h"
#include <string.h>
#include <stdlib.h>
/**
 * _atoi - Converts string to an integer
 * @s: variable char
 * Return: 0 if no string, and integer when their is a string
 */
int _atoi(char *s)
{
int val;
val = atoi(s);
return (val);
}
